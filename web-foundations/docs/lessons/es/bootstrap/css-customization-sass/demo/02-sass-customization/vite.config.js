import { defineConfig } from 'vite';

export default defineConfig({
	// Configuración básica para Vite con Sass
	css: {
		preprocessorOptions: {
			scss: {
				// Opciones adicionales de Sass si las necesitas
			},
		},
	},
});
